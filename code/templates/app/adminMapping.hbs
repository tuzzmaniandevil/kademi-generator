(function (g) {
    controllerMappings
        .adminController()
        .enabled(true)
        .pathSegmentName('{{camelCase appName}}')
        .defaultView(views.templateView('/theme/apps/{{appName}}/{{camelCase appName}}Example.html'))
        .build();
})(this);